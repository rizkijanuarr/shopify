<html>
    <head>
        <title>{{ page_title }}</title>
        {{ content_for_header }}
        <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet" type="text/css" media="all" />
    </head>

    <body>
        {% section 'header' %}
        {{ content_for_layout }}
    </body>
</html>   